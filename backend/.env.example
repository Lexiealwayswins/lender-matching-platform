# =============================================================================
# Lender Matching Platform - .env.example
# Copy this file to .env and modify the values for your environment.
# Never commit the real .env file to version control.
# =============================================================================

# --------------------- Database Configuration ---------------------
DATABASE_URL=postgresql://postgres:postgres123456@localhost:5432/lender_matching

# PostgreSQL Docker settings
POSTGRES_DB=lender_matching
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123456
POSTGRES_PORT=5432

# --------------------- Application Settings ---------------------
ENVIRONMENT=development

# --------------------- Hatchet Workflow Configuration (Optional) ---------------------
# These settings are only needed if you want to use the Hatchet orchestration workflow.
# You can obtain these values from https://hatchet.run after creating an account.

# HATCHET_CLIENT_TOKEN=hatchet_clie# HATCHET_CLIENT_TOKEN=hatRV# HATCHET_CLIENT_TOKEN=hatchet_clie# HATCHET_CLIENT_TOKEN=hatRV# HATCHET_CLIENdu# HATCHET_CLIENT_TOKEN=hatchet_clie# HA
# # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # mma-sep# # # # # # # # # # # # # # # # # # # # # # # # # # INS=http://localhost:5173,http://localhost:3000

# --------------------- Logging --# --------------------- Logging --# --------------------- Logg=======# --------------------- Logging --# --------------------- Logging --# ---------e: # ----------------nv
# 2. Update values (especially DATABASE_URL and POSTGRES_PA# 2. Update values (especiallt to use Hatchet, you can leave HATCHET_* lines commented out
# 4. Add .env to .gitignore (it should already be there)
# =============================================================================
